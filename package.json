
{
  "name": "grizzhacks-frontend",
  "version": "1.0.0",
  "private": true,
"homepage": "https://tmiester.github.io/grizzhacks/"
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "gh-pages -d build",   // Add this line
    "predeploy": "npm run build"     // Add this line
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
